app.controller('NavigationCtrl', function($scope, navigation) {

    $scope.navigation = navigation;

    /* $scope.$watch("navigation.headline", function(newVal, oldVal) {
        $scope.headline = newVal;
    }); */
});